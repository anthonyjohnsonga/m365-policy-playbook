# Keep the build context lean — the image only needs src/, www/,
# data/masters/, Start-PlaybookApp.ps1, and docker-entrypoint.sh.
.git
.github
.claude
.gitattributes
.gitignore
**/_backups
data/clients
reports
PNG
*.md
Launch.cmd
Launch.command
Dockerfile
.dockerignore
docker-compose.yml
